#pragma once

// Размеры окна
const int WINDOW_WIDTH = 1000;
const int WINDOW_HEIGHT = 600;

// Толщина линии
const float CENTER_LINE_WIDTH = 4.f;

// Отступ сверху
const float HUD_HEIGHT = 90.f;
const float SCORE_Y = 10.f;
const float BONUS_PANEL_Y = 55.f;

const float SCORE_OFFSET = 20.f;

const float PADDLE_SPEED = 450.f;

const float FIELD_MARGIN = 30.f;
const float FIELD_TOP = HUD_HEIGHT;
const float FIELD_LEFT = FIELD_MARGIN;
const float FIELD_RIGHT = WINDOW_WIDTH - FIELD_MARGIN;
const float FIELD_BOTTOM = WINDOW_HEIGHT - FIELD_MARGIN;

const float BALL_RADIUS = 10.f;

const int WIN_SCORE = 10;

const float PADDLE_WIDTH = 20.f;
const float PADDLE_HEIGHT = 100.f;
const float SMALL_PADDLE_HEIGHT = 60.f;

const float BALL_SPEED = 450.f;          
const float FAST_BALL_SPEED = 700.f;  

